---
interface Props {
  href: string;
  text: string;
  active: boolean;
}
const { href, text, active } = Astro.props;
---

<a class={active ? 'active' : ''} href={href}><span class="text-primary">#</span>{text}</a>
