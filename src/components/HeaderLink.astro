---
interface Props {
  href: string;
  text: string;
}
const { href, text } = Astro.props;
const isSection = href.startsWith('#');
---

<a class="nav-link" href={href} data-section={isSection ? href.replace('#', '') : href}
  ><span class="text-primary">#</span>{text}</a
>
