---
interface Props {
  class?: string;
}
const { class: dotsClass = '' } = Astro.props;
const dots = Array.from({ length: 25 });
---

<div class={`grid-rows-5h-12 grid max-w-fit grid-cols-5 gap-4 ${dotsClass}`}>
  {dots.map(() => <div class="bg-primary-gray h-1 w-1 rounded-full" />)}
</div>
